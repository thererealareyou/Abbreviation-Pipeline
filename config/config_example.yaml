database:
  user: ${POSTGRES_USER}
  password: ${POSTGRES_PASSWORD}
  host: ${POSTGRES_HOST}
  port: ${POSTGRES_PORT}
  db: ${POSTGRES_DB}

rabbitmq:
  user: ${RABBITMQ_USER}
  password: ${RABBITMQ_PASSWORD}
  host: ${RABBITMQ_HOST}
  port: ${RABBITMQ_PORT}

llm:
  host: ${LLM_API_HOST}
  port: ${LLM_API_PORT}

pipeline:
  batch_size: 10